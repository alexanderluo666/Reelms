# Riftfall
This is Riftall, a shooter consisting of multiple projects using C++, Raylib, JS, HTML and CSS.
A simple version.

Giving permission:
```bash
chmod +x riftfall
chmod +x  run.sh
```

Run build for CPP by:
```bash
./riftfall
```

Build after making your version:
```bash
./run.sh
```
